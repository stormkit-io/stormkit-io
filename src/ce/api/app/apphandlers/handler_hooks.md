# App hooks

The app hooks are a way to provide an endpoint for APIs to interact with Stormkit.
For instance, you could use a hook to trigger a deployment whenever you want. It
does not require authentication, however, it has a secret token that needs to be present
in order to continue with hooks.
